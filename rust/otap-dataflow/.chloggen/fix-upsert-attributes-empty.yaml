change_type: bug_fix

component: pdata

note: Return the input batch unchanged instead of panicking when `upsert_attributes` is called with no upserts

issues: [3836]

subtext: |
  An assignment whose source attributes are all absent resolves to zero upserts, and the
  transform then indexed an empty slice. This panicked the pipeline that ran the attributes
  processor.
